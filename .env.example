# Database Configuration
# Fallback is SQLite (sqlite:///crm.db) if this variable is left empty or commented out.

# PostgreSQL Connection String Format:
# postgresql://<username>:<password>@<host>:<port>/<database_name>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/team_crm
